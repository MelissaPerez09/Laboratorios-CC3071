(* YALex con dificultad baja *)

(* Introducir cualquier header aqui *)

let digits = ['0'-'4']
let regex = digits[abc]*

rule tokens = 
  | regex     { print regex }
  | '\n'      { print new line }

(* Introducir cualquier trailer aqui *)